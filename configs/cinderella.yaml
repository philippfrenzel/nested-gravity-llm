seed: 42
model: nested_gravity
task: text
vocab_size: 32
embedding_dim: 64
hidden_dim: 64
num_layers: 2
dropout: 0.0
max_sequence_length: 256
gravity_dim: 16
num_centers: 8
num_parent_centers: 2
local_window: 16
epsilon: 0.1
gravity_power: 1.0
force_clip: 5.0
temperature: 0.5
ema_alpha: 0.05
learning_rate: 0.0003
weight_decay: 0.0001
gradient_clip_norm: 1.0
epochs: 40
batch_size: 16
steps_per_epoch: 60
val_steps: 15
early_stopping_patience: 10
memory_length: 8
gap_length: 32
num_pairs: 4
max_depth: 4
noise_tokens: 0
text_sequence_length: 128
center_loss_weight: 0.001
entropy_loss_weight: 0.0
repulsion_loss_weight: 0.0
use_local_gravity: true
use_nesting: true
use_nested_bridges: false
bridge_strength: 1.0
use_repulsion: false
output_root: outputs
text_path: data/cinderella_expanded.txt
